apiVersion: v1
kind: Service
metadata:
  name: {{ include "dhis2-core-helm.fullname" . }}-hl
  labels:
    {{- include "dhis2-core-helm.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dhis2-core-helm.selectorLabels" . | nindent 4 }}
