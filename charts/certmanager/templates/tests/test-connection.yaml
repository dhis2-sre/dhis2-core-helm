apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "certmanager.fullname" . }}-test-connection"
  labels:
    {{- include "certmanager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['/bin/bash', '-c', 'echo "Installed Issuers successfully."']
  restartPolicy: Never
